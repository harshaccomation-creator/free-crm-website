import { useEffect, useMemo, useState } from 'react';
import DashboardSidebar from '../../components/dashboard/DashboardSidebar.jsx';
import { createLead, isBackendConfigured, listLeads } from '../../services/crmApi.js';
import '../../styles/leadActivityStable.css';
import '../../styles/leadListFontFix.css';
import '../../styles/leadListSoftProfessional.css';
import '../../styles/leadFilterFix.css';
import '../../styles/leadListFinalTableFix.css';

const sourceClass = { Website: 'blue', Referral: 'green', LinkedIn: 'purple', 'Cold Call': 'orange', 'Email Campaign': 'purple', WhatsApp: 'green', Other: 'blue' };
const statusClass = { New: 'blue', Contacted: 'orange', 'In Progress': 'blue', Converted: 'green', Won: 'green', Lost: 'red', 'Demo Done': 'green', 'Demo Scheduled': 'orange', 'Not Connected': 'red', 'Follow-up': 'orange' };
const sourceOptions = ['All Sources', 'Website', 'Referral', 'LinkedIn', 'Cold Call', 'Email Campaign', 'WhatsApp', 'Other'];
const statusOptions = ['All Statuses', 'New', 'Contacted', 'In Progress', 'Converted', 'Won', 'Lost', 'Demo Scheduled', 'Demo Done', 'Not Connected', 'Follow-up'];
const dateOptions = [{ label: 'All Dates', value: 'all' }, { label: 'Last 7 Days', value: 'last-7' }, { label: 'Today', value: 'today' }];
const defaultForm = { name: '', email: '', phone: '', company: '', source: 'Website', status: 'New', nextFollowUpDate: '', nextFollowUpTime: '', priority: 'Warm', jobTitle: '', notes: '' };
function LeadSvg({ type }) { const icons = { user:<path d="M20 21a8 8 0 0 0-16 0M12 13a5 5 0 1 0 0-10 5 5 0 0 0 0 10Z"/>, plus:<path d="M12 5v14M5 12h14"/>, phone:<path d="M22 16.9v3a2 2 0 0 1-2.2 2A19.8 19.8 0 0 1 3.1 5.2 2 2 0 0 1 5.1 3h3a2 2 0 0 1 2 1.7c.1.9.3 1.7.6 2.5a2 2 0 0 1-.5 2.1L9 10.5a16 16 0 0 0 4.5 4.5l1.2-1.2a2 2 0 0 1 2.1-.5c.8.3 1.6.5 2.5.6a2 2 0 0 1 1.7 2Z"/>, clock:<path d="M12 21a9 9 0 1 0-9-9 9 9 0 0 0 9 9Zm0-14v5l4 2"/>, crown:<path d="m3 8 4 3 5-7 5 7 4-3-2 10H5L3 8Zm2 13h14"/>, ban:<path d="M12 21a9 9 0 1 0-9-9 9 9 0 0 0 9 9ZM5.7 5.7l12.6 12.6"/>, download:<path d="M12 3v12m0 0 4-4m-4 4-4-4M5 21h14"/>, filter:<path d="M4 5h16l-6 7v5l-4 2v-7L4 5Z"/>, search:<path d="m21 21-4.3-4.3M10.5 18a7.5 7.5 0 1 1 0-15 7.5 7.5 0 0 1 0 15Z"/>, calendar:<path d="M7 3v4m10-4v4M4 9h16M5 5h14a1 1 0 0 1 1 1v14H4V6a1 1 0 0 1 1-1Z"/>, eye:<><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12Z"/><circle cx="12" cy="12" r="3"/></> }; return <svg className="la-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">{icons[type]}</svg>; }
function getCurrentRole() { return localStorage.getItem('salesflow_user_role') || localStorage.getItem('salesflowRole') || 'employee'; }
function formatDateTime(value = new Date()) { if (!value || value === '-') return '-'; const date = value instanceof Date ? value : new Date(value); if (Number.isNaN(date.getTime())) return String(value); return date.toLocaleString('en-IN', { day:'2-digit', month:'short', year:'numeric', hour:'2-digit', minute:'2-digit', hour12:true }).replace(',', ''); }
function makeInitials(name = '') { const parts = name.trim().split(/\s+/).filter(Boolean); return ((parts[0]?.[0] || 'L') + (parts[1]?.[0] || '')).toUpperCase(); }
function csvEscape(value) { return `"${String(value ?? '').replaceAll('"', '""')}"`; }
function parseLeadDate(text = '') { const match = String(text).replace(',','').match(/(\d{1,2})\s+([A-Za-z]{3})\s+(\d{4})/); if (!match) return null; const months={Jan:0,Feb:1,Mar:2,Apr:3,May:4,Jun:5,Jul:6,Aug:7,Sep:8,Oct:9,Nov:10,Dec:11}; return new Date(Number(match[3]), months[match[2]] ?? 0, Number(match[1])); }
function matchesDateRange(lead, range) { if (range === 'all') return true; const date = parseLeadDate(lead.lastActivity) || parseLeadDate(lead.nextFollowUp); if (!date) return false; if (range === 'today') return date.toDateString() === new Date().toDateString(); if (range === 'last-7') { const weekAgo = new Date(); weekAgo.setDate(weekAgo.getDate()-7); return date >= weekAgo; } return true; }
function normalizeLead(record) { return { id: record.id, initials: record.initials || makeInitials(record.name), name: record.name || 'Unnamed Lead', phone: record.phone || '-', email: record.email || '-', company: record.company || record.company_name || '-', source: record.source || 'Website', status: record.status === 'Won' ? 'Converted' : (record.status || 'New'), owner: record.owner?.full_name || record.owner || 'Unassigned', lastActivity: formatDateTime(record.last_activity_at || record.updated_at || record.created_at), nextFollowUp: record.next_follow_up ? formatDateTime(record.next_follow_up) : '-', priority: record.priority || 'Warm', jobTitle: record.job_title || record.jobTitle || 'Customer', notes: record.notes || '', raw: record }; }
function buildMetrics(rows) { return [['Total Leads', rows.length, 'Live data', 'user', 'blue'], ['New Leads', rows.filter(l=>l.status==='New').length, 'Live data', 'plus', 'blue'], ['Contacted', rows.filter(l=>l.status==='Contacted').length, 'Live data', 'phone', 'green'], ['In Progress', rows.filter(l=>l.status==='In Progress').length, 'Live data', 'clock', 'orange'], ['Converted', rows.filter(l=>['Converted','Won','Demo Done'].includes(l.status)).length, 'Live data', 'crown', 'green'], ['Lost', rows.filter(l=>l.status==='Lost').length, 'Live data', 'ban', 'red']]; }
function getFollowUpIso(form) { if (!form.nextFollowUpDate) return null; return new Date(`${form.nextFollowUpDate}T${form.nextFollowUpTime || '10:00'}`).toISOString(); }
export default function LeadListPage() { const role=getCurrentRole(); const [leadRows,setLeadRows]=useState([]); const [isLiveData,setIsLiveData]=useState(false); const [isLoading,setIsLoading]=useState(Boolean(isBackendConfigured)); const [dataMessage,setDataMessage]=useState(''); const [errorMessage,setErrorMessage]=useState(''); const [isAddOpen,setIsAddOpen]=useState(false); const [isSaving,setIsSaving]=useState(false); const [form,setForm]=useState(defaultForm); const [createdAt,setCreatedAt]=useState(formatDateTime()); const [currentPage,setCurrentPage]=useState(1); const [pageSize,setPageSize]=useState(10); const [dateRange,setDateRange]=useState('all'); const [sourceFilter,setSourceFilter]=useState('All Sources'); const [statusFilter,setStatusFilter]=useState('All Statuses'); const [searchTerm,setSearchTerm]=useState('');
  async function loadRealLeads(){ if(!isBackendConfigured){ setIsLoading(false); setErrorMessage('Supabase environment variables missing.'); return; } try{ setIsLoading(true); const rows=await listLeads({limit:1000}); setLeadRows(rows.map(normalizeLead)); setIsLiveData(true); setDataMessage(rows.length?'Live Supabase data connected.':'Live Supabase connected. No leads found yet.'); }catch(error){ setIsLiveData(false); setErrorMessage(error.message || 'Unable to load leads.'); }finally{ setIsLoading(false); } }
  useEffect(()=>{ loadRealLeads(); },[]);
  const filteredLeads=useMemo(()=>leadRows.filter(lead=>{ const q=searchTerm.trim().toLowerCase(); const searchMatch=!q||[lead.name,lead.phone,lead.email,lead.company,lead.source,lead.status,lead.owner].some(v=>String(v||'').toLowerCase().includes(q)); return searchMatch && (sourceFilter==='All Sources'||lead.source===sourceFilter) && (statusFilter==='All Statuses'||lead.status===statusFilter) && matchesDateRange(lead,dateRange); }),[leadRows,searchTerm,sourceFilter,statusFilter,dateRange]);
  const metrics=useMemo(()=>buildMetrics(leadRows),[leadRows]); const totalPages=Math.max(1,Math.ceil(filteredLeads.length/pageSize)); const safePage=Math.min(currentPage,totalPages); const pageStart=(safePage-1)*pageSize; const pageEnd=Math.min(pageStart+pageSize,filteredLeads.length); const visibleLeads=filteredLeads.slice(pageStart,pageEnd); const activeDateLabel=dateOptions.find(i=>i.value===dateRange)?.label||'All Dates';
  const resetToFirstPage=()=>setCurrentPage(1); const openLead=(id)=>{ window.history.pushState({},'',`/leads/${id}`); window.dispatchEvent(new Event('salesflow:navigate')); }; const openAddLead=()=>{ setCreatedAt(formatDateTime()); setForm(defaultForm); setErrorMessage(''); setDataMessage(''); setIsAddOpen(true); }; const updateForm=(key,value)=>setForm(c=>({...c,[key]:value}));
  const saveLead=async(event)=>{ event.preventDefault(); if(isSaving)return; setErrorMessage(''); if(!form.name.trim())return setErrorMessage('Lead name is required.'); if(!form.phone.trim())return setErrorMessage('Phone number is required.'); try{ setIsSaving(true); const saved=await createLead({ name:form.name.trim(), email:form.email.trim(), phone:form.phone.trim(), company:form.company.trim(), source:form.source, status:form.status, priority:form.priority, job_title:form.jobTitle||'Customer', notes:form.notes, next_follow_up:getFollowUpIso(form) }); setLeadRows(rows=>[normalizeLead(saved),...rows]); setCurrentPage(1); setDateRange('all'); setSourceFilter('All Sources'); setStatusFilter('All Statuses'); setSearchTerm(''); setIsAddOpen(false); setForm(defaultForm); setIsLiveData(true); setDataMessage('Lead saved to Supabase.'); }catch(error){ setErrorMessage(error.message||'Unable to save lead.'); }finally{ setIsSaving(false); } };
  const exportLeads=()=>{ const headers=['Lead Name','Email','Mobile Number','Company','Lead Source','Status','Lead Owner','Created Date Time','Next Follow Up','Priority','Job Title']; const rows=filteredLeads.map(l=>[l.name,l.email,l.phone,l.company,l.source,l.status,l.owner,l.lastActivity,l.nextFollowUp,l.priority,l.jobTitle]); const csv=[headers,...rows].map(row=>row.map(csvEscape).join(',')).join('\n'); const blob=new Blob([`\uFEFF${csv}`],{type:'text/csv;charset=utf-8;'}); const url=URL.createObjectURL(blob); const link=document.createElement('a'); link.href=url; link.download=`salesflow-leads-${new Date().toISOString().slice(0,10)}.csv`; document.body.appendChild(link); link.click(); link.remove(); URL.revokeObjectURL(url); };
  return <div className="sf-dashboard la-page"><DashboardSidebar role={role}/><main className="la-main"><header className="la-header"><div><h1>My Leads</h1><p>Home › Leads</p></div><div className="la-actions"><button type="button" onClick={exportLeads}><LeadSvg type="download"/>Export</button><button type="button" className="primary" onClick={openAddLead}>+ Add Lead</button></div></header>{errorMessage?<div className="la-live-banner demo">{errorMessage}</div>:null}{dataMessage?<div className={`la-live-banner ${isLiveData?'live':'demo'}`}>{isLoading?'Loading leads...':dataMessage}</div>:null}<section className="la-metrics">{metrics.map(([title,value,change,icon,tone])=><article className={`la-metric ${tone}`} key={title}><span className="la-metric-icon"><LeadSvg type={icon}/></span><div><p>{title}</p><h2>{isLoading?'...':value}</h2><small>{change}</small></div></article>)}</section><section className="la-filters"><label className="la-field la-filter-control"><span>Date Range</span><strong>{activeDateLabel}</strong><select value={dateRange} onChange={(e)=>{setDateRange(e.target.value);resetToFirstPage();}}>{dateOptions.map(i=><option key={i.value} value={i.value}>{i.label}</option>)}</select><i>⌄</i></label><label className="la-field la-filter-control"><span>Lead Source</span><strong>{sourceFilter}</strong><select value={sourceFilter} onChange={(e)=>{setSourceFilter(e.target.value);resetToFirstPage();}}>{sourceOptions.map(i=><option key={i}>{i}</option>)}</select><i>⌄</i></label><label className="la-field la-filter-control"><span>Status</span><strong>{statusFilter}</strong><select value={statusFilter} onChange={(e)=>{setStatusFilter(e.target.value);resetToFirstPage();}}>{statusOptions.map(i=><option key={i}>{i}</option>)}</select><i>⌄</i></label><label className="la-search"><LeadSvg type="search"/><input value={searchTerm} onChange={(e)=>{setSearchTerm(e.target.value);resetToFirstPage();}} placeholder="Search leads..."/></label><button className="la-filter-btn" type="button" onClick={()=>{setDateRange('all');setSourceFilter('All Sources');setStatusFilter('All Statuses');setSearchTerm('');setCurrentPage(1);}}><LeadSvg type="filter"/>Reset</button></section><section className="la-table-card"><table className="la-table"><thead><tr><th><input className="la-check" type="checkbox"/></th><th>Lead Name</th><th>Company</th><th>Lead Source</th><th>Status</th><th>Last Activity</th><th>Next Follow Up</th><th>Lead Owner</th><th>Actions</th></tr></thead><tbody>{visibleLeads.map(lead=><tr key={lead.id} onClick={()=>openLead(lead.id)}><td><input className="la-check" type="checkbox" onClick={(e)=>e.stopPropagation()}/></td><td><div className="la-person"><span className="la-avatar">{lead.initials}</span><div><strong>{lead.name}</strong><small>{lead.phone}</small></div></div></td><td>{lead.company}</td><td><span className={`la-pill ${sourceClass[lead.source]||'blue'}`}>{lead.source}</span></td><td><span className={`la-pill ${statusClass[lead.status]||'blue'}`}>{lead.status}</span></td><td><span className="la-icon-text"><LeadSvg type="phone"/>{lead.lastActivity}</span></td><td><span className="la-icon-text"><LeadSvg type="calendar"/>{lead.nextFollowUp}</span></td><td><span className="la-owner"><span className="la-avatar">{String(lead.owner||'U')[0]}</span>{lead.owner}</span></td><td><button type="button" className="la-row-action" onClick={(e)=>{e.stopPropagation();openLead(lead.id);}}><LeadSvg type="eye"/></button></td></tr>)}</tbody></table><footer className="la-footer"><span>{filteredLeads.length?`Showing ${pageStart+1} to ${pageEnd} of ${filteredLeads.length} leads`:'Showing 0 leads'}</span><div className="la-pagination"><button type="button" disabled={safePage===1} onClick={()=>setCurrentPage(p=>Math.max(1,p-1))}>‹</button><button type="button" className="active">{safePage}</button><button type="button" disabled={safePage===totalPages} onClick={()=>setCurrentPage(p=>Math.min(totalPages,p+1))}>›</button><select value={pageSize} onChange={(e)=>{setPageSize(Number(e.target.value));setCurrentPage(1);}}><option value="5">5 / page</option><option value="10">10 / page</option><option value="20">20 / page</option><option value="50">50 / page</option></select></div></footer></section></main>{isAddOpen&&<div className="la-modal-backdrop" role="presentation" onClick={()=>setIsAddOpen(false)}><section className="la-modal" role="dialog" aria-modal="true" aria-labelledby="addLeadTitle" onClick={(e)=>e.stopPropagation()}><header><div><h2 id="addLeadTitle">Add New Lead</h2><p>Created automatically on {createdAt}</p></div><button type="button" onClick={()=>setIsAddOpen(false)}>×</button></header>{errorMessage?<div className="la-live-banner demo">{errorMessage}</div>:null}<form onSubmit={saveLead} className="la-form"><label>Customer Name<input required value={form.name} onChange={(e)=>updateForm('name',e.target.value)} placeholder="Enter customer name"/></label><label>Email ID<input type="email" value={form.email} onChange={(e)=>updateForm('email',e.target.value)} placeholder="customer@example.com"/></label><label>Mobile Number<input required value={form.phone} onChange={(e)=>updateForm('phone',e.target.value)} placeholder="+91 98765 43210"/></label><label>Company<input value={form.company} onChange={(e)=>updateForm('company',e.target.value)} placeholder="Company name"/></label><label>Lead Source<select value={form.source} onChange={(e)=>updateForm('source',e.target.value)}><option>Website</option><option>Referral</option><option>LinkedIn</option><option>Cold Call</option><option>Email Campaign</option><option>WhatsApp</option><option>Other</option></select></label><label>Status<select value={form.status} onChange={(e)=>updateForm('status',e.target.value)}><option>New</option><option>Contacted</option><option>In Progress</option><option>Converted</option><option>Lost</option><option>Demo Scheduled</option><option>Demo Done</option><option>Not Connected</option><option>Follow-up</option></select></label><label>Next Follow-up Date<input type="date" value={form.nextFollowUpDate} onChange={(e)=>updateForm('nextFollowUpDate',e.target.value)}/></label><label>Next Follow-up Time<input type="time" value={form.nextFollowUpTime} onChange={(e)=>updateForm('nextFollowUpTime',e.target.value)}/></label><label>Priority<select value={form.priority} onChange={(e)=>updateForm('priority',e.target.value)}><option>Hot</option><option>Warm</option><option>High</option><option>Medium</option><option>Low</option></select></label><label>Job Title<input value={form.jobTitle} onChange={(e)=>updateForm('jobTitle',e.target.value)} placeholder="Founder / Manager / Customer"/></label><label className="full">Notes<textarea value={form.notes} onChange={(e)=>updateForm('notes',e.target.value)} placeholder="Requirement, budget, interest, discussion notes..."/></label><footer><button type="button" onClick={()=>setIsAddOpen(false)}>Cancel</button><button className="primary" type="submit" disabled={isSaving}>{isSaving?'Saving...':'Save Lead'}</button></footer></form></section></div>}</div>; }
